<!DOCTYPE html>
<html>
  <head>
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title><%= content_for?(:title) ? yield(:title) : "Sistemaescola" %></title>
    <meta name="description" content="<%= content_for?(:description) ? yield(:description) : "Sistemaescola" %>">
    <%= stylesheet_link_tag "application", :media => "all" %>
    <%= javascript_include_tag "application" %>
    <%= csrf_meta_tags %>
    <%= yield(:head) %>
  </head>
  <p>
    <ul class="nav nav-list">
      <li class="active">
        <a href="/disciplinas"><i class="icon-home icon-white icon-book"></i> Home - Disciplinas </a>
      </li>
      <li>
        <a href="/professors"><i class="icon-user icon-briefcase"></i>Professors</a>
      </li>
      <li>
        <a href="/alunos"><i class="icon-user"></i>Alunos</a>
      </li>
      <li>
        <a href="/turmas"><i class="icon-th-list"></i>Turmas</a>
      </li>
    </ul>
  </p>
  <body class="<%= controller_name %> <%= action_name %>">
    <header class="navbar navbar-fixed-top">
      <nav class="navbar-inner">
        <div class="container">
          <%= render 'layouts/navigation' %>
        </div>
      </nav>
    </header>
    <div id="main" role="main">
      <div class="container">
        <div class="content">
           <div class="row">
            <div class="span12">
              <%= render 'layouts/messages' %>
              <%= yield %>
            </div>
          </div>
          <footer>
          </footer>
        </div>
      </div> <!--! end of .container -->
    </div> <!--! end of #main -->
  </body>
</html>
